---
apiVersion: v1
kind: Service
metadata:
  name: hystrix-dashboard
  labels:
    name: hystrix-dashboard
  namespace: my-store
spec:
  ports:
    # the port that this service should serve on
  - port: 6161
    targetPort: 6161
  selector:
    name: hystrix-dashboard