spring:
  profiles:
    active: default,localhost
---
spring:
  profiles: default
server:
  port: 18021
security:
  enable-csrf: false
  ignored: /assets/**
management:
  security:
    enabled: false
---
spring:
  profiles: localhost
zuul:
  ignored-services: '*'
  routes:
    edge-service:
      path: /api/**
      url: http://localhost:18020
    auth-service:
      path: /user/**
      url: http://localhost:18027/uaa/user
security:
  oauth2:
    resource:
      userInfoUri: http://localhost:18027/uaa/user
    client:
      accessTokenUri: http://localhost:18027/uaa/oauth/token
      userAuthorizationUri: http://localhost:18027/uaa/oauth/authorize
      clientId: acme
      clientSecret: acmesecret
